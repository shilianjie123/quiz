spring:
  datasource:
    #    数据源基本配置
    username: root    #前面没有data直接是username或password
    password: root
    url: jdbc:mysql://localhost:3306/quiz?useUnicode=true&characterEncoding=utf-8&useSSL=false&&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    #   数据源其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall  #,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
  #    schema:
  #      - classpath:sql/books.sql
  #    initialization-mode: always
  thymeleaf:
    prefix: classpath:/templates/
    cache: false
    suffix: .html
    encoding: UTF-8
    mode: HTML
    servlet:
      content-type: text/html

  redis:
    database: 0
    host: localhost
    port: 6379
#    password: root
    jedis:
      pool:
        max-active: 20
        max-wait: -1
        max-idle: 10
        min-idle: 0
        timeout: 5000
#开发时让无缓存
  mail:
    host: smtp.qq.com
    username: 965817262@qq.com
    password: swomdsqplhpvbdjc
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
          starttls.required: true
          ssl.enable: true
          port: 465
    port: 465
    default-encoding: UTF-8

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

mybatis:
  type-aliases-package: com.zhao.quiz.domain
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

register:
  subject: "线上答题小助手"
  contentV1: "验证码有效时间为2分钟，请您注意把握时间，您的注册验证码为:"
  contentV2: "您之前获取的验证码还未过期，所以本次不会获取新的验证码，请注意过期时间，您的注册码为："

ftp:
  host: 49.235.229.157
  userName: ubuntu
  password: Bc-1293465476.
  port: 22
  rootPath: /home/ubuntu/video_test
  video:
    url: http://49.235.229.157:80
